{
  "name": "chat-bot",
  "version": "1.0.0",
  "description": "A chat bot that sends icebreaker messages",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "postinstall": "npx playwright install --with-deps",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["chat", "bot", "playwright", "automation"],
  "author": "Chat Bot Developer",
  "license": "MIT",
  "dependencies": {
    "playwright": "^1.40.0",
    "dotenv": "^16.3.1"
  }
}